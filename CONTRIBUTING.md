# First-time Git setup

Git was not available in the build environment. After installing [Git for Windows](https://git-scm.com/download/win):

```powershell
cd "c:\Users\Dell Latitude\OneDrive\Documents\App codes\Job hunter"
git init
git add .
git commit -m "Initial commit: JobHunter AI platform with Supabase auth and LangGraph matching"
```
